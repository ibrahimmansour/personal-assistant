---
description: Run production build and fix any errors
---

Run `npm run build` and analyze the output. If there are TypeScript errors, ESLint errors, or build failures:

1. List all errors with their file locations
2. Fix each error systematically
3. Re-run the build to verify the fix

Do NOT skip or suppress errors. Fix the root cause.
