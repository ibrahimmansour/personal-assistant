---
description: Start the dev server (Next.js + PTY) and watch for errors
---

Run `npm run dev` to start both the Next.js dev server (port 4444) and the PTY WebSocket server (port 4445). Watch the output for any compilation errors or warnings. If there are errors, diagnose and fix them. Report the dev server status when ready.
