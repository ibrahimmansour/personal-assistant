---
description: Run ESLint and fix all issues
---

Run `npm run lint` to check for ESLint issues. If there are any:

1. List all lint errors and warnings with file locations
2. Fix each issue (prefer auto-fixable changes)
3. Re-run lint to verify everything passes

Focus on actual code quality issues, not just formatting.
